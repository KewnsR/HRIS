@page
@model HumanRepProj.Pages.UserRegisterModel
@{
    ViewData["Title"] = "User Registration";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HRIS - Register</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            font-family: 'Arial', sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
            padding: 0;
        }

        .register-card {
            width: 100%;
            max-width: 450px;
            background: #fff;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .card-header {
            background: linear-gradient(135deg, #212529);
            color: #fff;
            text-align: center;
            padding: 25px 20px;
        }

            .card-header h3 {
                margin: 0;
                font-size: 1.6rem;
            }

            .card-header p {
                margin: 5px 0 0;
                font-size: 0.95rem;
                color: rgba(255, 255, 255, 0.85);
            }

        .card-body {
            padding: 30px;
        }

        .form-group {
            margin-bottom: 20px;
        }

            .form-group label {
                display: block;
                margin-bottom: 8px;
                font-size: 14px;
                font-weight: 500;
                color: #495057;
            }

        .form-control {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #e1e5eb;
            border-radius: 10px;
            font-size: 14px;
            box-sizing: border-box;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

            .form-control:focus {
                border-color: #6d7fcc;
                box-shadow: 0 0 0 3px rgba(109, 127, 204, 0.15);
                outline: none;
            }

        .password-field {
            position: relative;
        }

        .password-toggle {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #6c757d;
        }

        .password-requirements {
            font-size: 12px;
            color: #6c757d;
            margin-top: 5px;
        }

        .btn-register {
            width: 100%;
            padding: 12px;
            border-radius: 10px;
            background: linear-gradient(135deg, #212529);
            color: #fff;
            font-weight: bold;
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s;
        }

            .btn-register:hover {
                background: linear-gradient(135deg, #328E6E);
                box-shadow: 0 5px 15px rgba(78, 115, 223, 0.4);
            }

            .btn-register i {
                margin-right: 8px;
            }

        .login-link {
            text-align: center;
            margin-top: 20px;
        }

        .login-account-link {
            color: #4e73df;
            font-weight: 500;
            text-decoration: none;
        }

            .login-account-link:hover {
                color: #8E54E9;
            }

        .separator {
            display: flex;
            align-items: center;
            text-align: center;
            margin: 25px 0;
        }

            .separator::before,
            .separator::after {
                content: '';
                flex: 1;
                border-bottom: 1px solid #e1e5eb;
            }

            .separator span {
                padding: 0 10px;
                color: #6c757d;
                font-size: 14px;
            }

        .social-register {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-bottom: 20px;
        }

        .social-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            border: 1px solid #e1e5eb;
            background: #fff;
            color: #6c757d;
            font-size: 18px;
            cursor: pointer;
            transition: all 0.3s;
        }

            .social-btn:hover {
                background: #f8f9fa;
                transform: translateY(-2px);
            }


        .error-message {
            color: #dc3545;
            font-size: 0.85rem;
            margin-top: 0.5rem;
            background-color: rgba(220, 53, 69, 0.1);
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 15px;
        }

        .alert {
            padding: 12px;
            margin-bottom: 20px;
            border-radius: 8px;
            display: flex;
            align-items: center;
        }

        .alert-danger {
            background-color: rgba(220, 53, 69, 0.1);
            color: #dc3545;
            border: 1px solid rgba(220, 53, 69, 0.2);
        }

        .alert i {
            margin-right: 10px;
            font-size: 16px;
        }

        .btn-close {
            margin-left: auto;
            background: transparent;
            border: none;
            font-size: 20px;
            cursor: pointer;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="register-card">
        <div class="card-header">
            <h3>Create Account</h3>
            <p>Please fill in your information to register</p>
        </div>
        <div class="card-body">
            @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
            {
                <div class="alert alert-danger" role="alert">
                    <i class="fas fa-exclamation-circle"></i>
                    <span>@Model.ErrorMessage</span>
                </div>
            }

            @if (ViewData.ModelState.ErrorCount > 0)
            {
                <div class="alert alert-danger" role="alert">
                    <i class="fas fa-exclamation-circle"></i>
                    <span>Please correct the errors below</span>
                </div>
            }

            <form method="post">
                <div class="form-group">
                    <label asp-for="Input.FirstName">First Name</label>
                    <input asp-for="Input.FirstName" class="form-control" placeholder="Enter first name" required />
                    <span asp-validation-for="Input.FirstName" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.LastName">Last Name</label>
                    <input asp-for="Input.LastName" class="form-control" placeholder="Enter last name" required />
                    <span asp-validation-for="Input.LastName" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.Email">Email Address</label>
                    <input asp-for="Input.Email" type="email" class="form-control" placeholder="name@example.com" required />
                    <span asp-validation-for="Input.Email" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.PhoneNumber">Phone Number</label>
                    <input asp-for="Input.PhoneNumber" class="form-control" placeholder="Optional" />
                    <span asp-validation-for="Input.PhoneNumber" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.Address">Address</label>
                    <input asp-for="Input.Address" class="form-control" placeholder="Optional" />
                    <span asp-validation-for="Input.Address" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.DateOfBirth">Date of Birth</label>
                    <input asp-for="Input.DateOfBirth" type="date" class="form-control" required />
                    <span asp-validation-for="Input.DateOfBirth" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.Gender">Gender</label>
                    <select asp-for="Input.Gender" class="form-control" required>
                        <option value="">Select gender</option>
                        <option value="Male">Male</option>
                        <option value="Female">Female</option>
                        <option value="Other">Other</option>
                    </select>
                    <span asp-validation-for="Input.Gender" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.DepartmentID">Department</label>
                    <select asp-for="Input.DepartmentID" asp-items="Model.DepartmentOptions" class="form-control" required>
                        <option value="">Select department</option>
                    </select>
                    <span asp-validation-for="Input.DepartmentID" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.Position">Position</label>
                    <input asp-for="Input.Position" class="form-control" placeholder="e.g. Software Engineer" required />
                    <span asp-validation-for="Input.Position" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.Salary">Salary</label>
                    <input asp-for="Input.Salary" type="number" min="0" step="0.01" class="form-control" required />
                    <span asp-validation-for="Input.Salary" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.DateHired">Date Hired</label>
                    <input asp-for="Input.DateHired" type="date" class="form-control" required />
                    <span asp-validation-for="Input.DateHired" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.EmploymentType">Employment Type</label>
                    <select asp-for="Input.EmploymentType" class="form-control">
                        <option value="Full-time">Full-time</option>
                        <option value="Part-time">Part-time</option>
                        <option value="Contract">Contract</option>
                        <option value="Probationary">Probationary</option>
                    </select>
                    <span asp-validation-for="Input.EmploymentType" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.Username">Username</label>
                    <input asp-for="Input.Username" class="form-control" placeholder="Choose a username" required />
                    <span asp-validation-for="Input.Username" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.Password">Password</label>
                    <div class="password-field">
                        <input asp-for="Input.Password" type="password" id="password" class="form-control" placeholder="Create a password" required />
                        <i class="fas fa-eye password-toggle" onclick="togglePasswordVisibility('password')"></i>
                    </div>
                    <div class="password-requirements">
                        Password must be at least 8 characters
                    </div>
                    <span asp-validation-for="Input.Password" class="error-message"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Input.ConfirmPassword">Confirm Password</label>
                    <div class="password-field">
                        <input asp-for="Input.ConfirmPassword" type="password" id="confirmPassword" class="form-control" placeholder="Confirm your password" required />
                        <i class="fas fa-eye password-toggle" onclick="togglePasswordVisibility('confirmPassword')"></i>
                    </div>
                    <span asp-validation-for="Input.ConfirmPassword" class="error-message"></span>
                </div>

                <button type="submit" class="btn-register">
                    <i class="fas fa-user-plus"></i> Register Account
                </button>

                <div class="login-link">
                    <p>Already have an account? <a href="/UserLogin" class="login-account-link">Login here</a></p>
                </div>
            </form>
        </div>
    </div>

    <script>
        function togglePasswordVisibility(inputId) {
            const passwordInput = document.getElementById(inputId);
            const icon = passwordInput.nextElementSibling;

            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            } else {
                passwordInput.type = 'password';
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            }
        }
    </script>
    <partial name="_ValidationScriptsPartial" />
</body>
</html>